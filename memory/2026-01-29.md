# 2026-01-29

## Jerry 诞生日 🎉

- Vicente 给我起名 Jerry，emoji 💡
- 确定了工作方式：中文对话、英文搜索、简洁幽默风格
- 工作流程：倾听需求 → 澄清问题 → 制定计划 → 确认后执行 → 汇报结果
- 完成了 IDENTITY.md、USER.md、SOUL.md 的初始化

## Telegram 接入
- 配置 Telegram DM allowlist，Vicente user id: 7696507791
- Bot: @Jerry_molt_bot

## Ollama 本地模型接入
- Mac Studio M2 Max / 64GB
- 安装了 nemotron-3-nano (30B MoE, 3.5B active, 24GB)
- Moltbot 自动发现不生效（envMap 缺 ollama 映射），改用显式 provider 配置
- 需要同时加入 agents.defaults.models allowlist 才能切换
- 别名：nano，费用 $0
- 测试通过：中文、算术、模型识别都正常
- ⚠️ 必须设 reasoning: false，否则 sub-agent 调用会超时/报错（Ollama 不支持 think 参数）

## Google Antigravity
- 之前连接失败，token 已过期
- 计划回家后重新认证，加入 fallback 链（Anthropic → Antigravity → Nano）

## Moltbot 自动更新
- 从 2026.1.27-beta.1 升级到 2026.1.29 ✅
- 完整流程：fetch → preflight（worktree + install + lint + build）→ rebase → install → build → ui:build → doctor
- doctor 报告：Google Antigravity token 即将过期（32m）、state 目录权限建议 chmod 700
- Ollama 发现超时（不影响手动配置）
- restore control-ui 步骤报错（pathspec 不匹配），但整体更新成功

## 工作目录限制
- 只允许访问 ~/Workspace/ 和 /Users/vicenteliu/openclaw
- 不访问 Documents、Desktop、Downloads 或其他路径（隐私要求）

## Resume 项目 (Operation_Undock)
- Vicente 在整理简历，目标岗位: System Engineer
- 收到完整职业背景母本 → career_background.md
- 重写 cv.yaml：修正结构/日期/内容，基于母本+2021旧简历
- 补充：PXE细节、服务器管理、IT库房管理系统、3000+/7000+规模
- LinkedIn 全面更新完成：Headline、About、Experience、Skills、Certifications
- 创建了 MEMORY.md 长期记忆文件

## Google Antigravity Token 刷新
- 通过 `moltbot configure --section model` 完成 OAuth 刷新
- Fallback 链更新为：Antigravity → Anthropic → Nano
- 完成对应 Reminder

## OpenClaw 升级 (1/30)
- Moltbot → OpenClaw 改名，43 commits 更新
- doctor --fix 完成配置迁移
- 旧 launchd service `bot.molt.gateway` 已清除
- 新 service `ai.openclaw.gateway` 运行正常
- Telegram 通道测试通过 ✅
