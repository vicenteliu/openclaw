# 2026-02-14 (Saturday)

## Dexter 项目分析

Vicente 同步了 [Dexter](https://github.com/virattt/dexter) 到 `~/Workspace/dexter/`，这是一个自主金融研究 agent。

### 核心架构亮点（可借鉴到 Risk-First-Protocol）

1. **Scratchpad 系统** (`src/agent/scratchpad.ts`)
   - JSONL 追加写入所有 tool calls、结果、思考过程
   - 支持 context 管理和审计轨迹
   - 防止重试循环的 query similarity 检测

2. **Skills 系统** (`src/skills/`)
   - SKILL.md 定义 workflow（YAML frontmatter + markdown 指令）
   - DCF 估值 skill 是典型例子：8 步流程 + 自我验证
   - 参考数据：`sector-wacc.md`

3. **自我验证机制**
   - 执行前 sanity check
   - EV 对比、Terminal value ratio 检验

4. **数据源**: FinancialDatasets.ai (机构级，需付费 API)

### 对 Risk-First-Protocol 的改进建议

- 加入 Scratchpad 审计日志（记录每次交易决策的完整链路）
- 设计 skill 系统：`fundamental-check/`, `sentiment-analysis/`
- 增加执行前验证步骤

---

## Risk-First Protocol 状态

- 全天 HOLD 信号，无交易执行
- 组合价值在 $1,900-$1,920 区间波动
- 市场下行趋势持续 (EMA50 < EMA200)
- BTC ~$69-70k, ETH ~$2,040-2,080

---

## Dexter 实测结果

### 环境配置
- Bun v1.3.9 安装成功
- FinancialDatasets.ai API 配置完成（$19.17 credit）
- AAPL/NVDA/MSFT 是免费 ticker

### 测试结果
- **Query**: "What is NVDA's latest revenue and operating margin?"
- **结果**: Revenue $187.1B (TTM Q3 2026), Operating Margin 57.8%
- **API 响应**: 5.7 秒
- **总耗时**: 36 秒（本地 Ollama ministral-3 处理慢）
- **Token**: 13,316 @ 372.6 tok/s

### 问题
- Google Gemini API key 过期（400 Bad Request）
- 尝试切换到 Vicente 的新 key：`[REDACTED]`
- 已更新到 `~/Workspace/dexter/.env`，待 Vicente 手动测试

### 结论
- FinancialDatasets.ai 数据质量不错，返回 TTM 和季度数据
- 本地 LLM 太慢，需要云端模型才实用
- Dexter 的 Scratchpad 和 SKILL.md 系统值得参考

---

## DCF 估值测试（Dexter + Gemini 3 Flash）

### 测试结果汇总

| 股票 | 当前价 | DCF 公允价值 | 下行空间 | WACC | 增速假设 |
|------|--------|-------------|---------|------|----------|
| NVDA | $182.78 | $60.22 | -67% | 9.5% | 15% |
| SOFI | $19.61 | $11.84 | -40% | 11.5% | 25% |
| PLTR | $131.36 | $29.47 | -78% | 9.0% | 35% |

### 模型亮点
- **假设透明**：每个输入都有来源说明
- **自我验证**：Sensitivity 分析 + Terminal Value 检查 + Caveats
- **自适应**：SOFI 识别为金融机构，用 Net Income 代替扭曲的 FCF

### Terminal Value 占比
- NVDA: 75.6% (略高但可接受)
- SOFI: 72.8% (可接受)
- PLTR: 83% ⚠️ (偏高)

### 投资启示
- 三股全部显示高估（AI 牛市溢价）
- PLTR 估值最极端（284x P/E, 80x Sales）
- 如果入场建议分批或等回调

### 集成建议
1. FinancialDatasets.ai → Wolf_Fintrack（数据源）
2. DCF Skill 结构 → Risk-First（入场参考）
3. Sensitivity 分析 → Google Sheets 报告

---

## OpenClaw 升级

- v2026.2.9 → **v2026.2.15**
- 新功能：nested subagent orchestration, IRC extension, thread-ownership
- Gateway 重启成功，所有服务正常

---

## Wolf_Fintrack DCF 模块集成（中期方案完成）

### 新增文件
```
Wolf_Fintrack/src/valuation/
├── __init__.py
├── dcf.py                         # DCF 计算核心 (17KB, 8 步流程)
├── financial_datasets_client.py   # FinancialDatasets.ai API 客户端
├── sector_wacc.py                 # 11 行业 WACC 表 + 调整因子
└── sheets_sync.py                 # Google Sheets 同步

Wolf_Fintrack/scripts/
├── dcf_valuation.py               # CLI: python scripts/dcf_valuation.py NVDA
└── dcf_to_sheets.py               # CLI: 运行 DCF + 同步到 Sheets
```

### 4 步集成完成

1. **Docker 容器集成** ✅
   - API key 添加到 docker-compose.yml
   - 通过 volume mount 自动可用

2. **Cron Job** ✅
   - 名称: `weekly-dcf-valuation`
   - 时间: 每周六 9:00 AM
   - 运行 DCF → 同步 Sheets → Telegram 简报

3. **Google Sheets 同步** ✅
   - 目标: Wolf_FinTrack → "DCF Valuation" 标签
   - 自动创建工作表和表头

4. **Risk-First-Protocol 联动** ✅
   - 新模块: `src/data/stock_valuation.py`
   - 集成到 `analyze.py` 输出
   - 市场风险等级: LOW/MODERATE/HIGH/EXTREME

### 运行效果
```
📊 Tech Stock Valuations (DCF):
   🔴 NVDA: $182.78 → $54.90 (-70.0%)
   🔴 AAPL: $255.78 → $81.27 (-68.2%)
   🔴 MSFT: $401.32 → $138.54 (-65.5%)
   Market Risk: EXTREME (avg -67.9%)
```

---

## Wolf_Fintrack Docker 配置备忘

### 容器信息
- **容器名**: `wolf_fintrack_collector`
- **镜像**: 自建 (`Dockerfile.collector`)
- **基础镜像**: `python:3.12-slim`

### docker-compose.yml 关键配置
```yaml
collector:
  build:
    context: .
    dockerfile: Dockerfile.collector
  container_name: wolf_fintrack_collector
  environment:
    - DATABASE_URL=postgresql://postgres:xxx@host.docker.internal:5432/fintrack
    - TZ=America/Los_Angeles
    - FINANCIAL_DATASETS_API_KEY=3e0c76e8-892f-47aa-a085-2a6802cc5a2c  # 新增
  volumes:
    - ./config.json:/app/config.json:ro
    - ./service_account.json:/app/service_account.json:ro
    - ./data:/app/data
    - ./logs:/app/logs
    - ./collector:/app/collector:ro
    - ./src:/app/src:ro          # DCF 模块通过这个挂载
    - ./scripts:/app/scripts:ro   # CLI 脚本通过这个挂载
```

### 新增文件（需要 volume mount 才能生效）
```
src/valuation/
├── __init__.py
├── dcf.py                         # DCF 核心计算
├── financial_datasets_client.py   # API 客户端
├── sector_wacc.py                 # WACC 参考表
└── sheets_sync.py                 # Sheets 同步

scripts/
├── dcf_valuation.py               # CLI: DCF 分析
└── dcf_to_sheets.py               # CLI: DCF + Sheets 同步
```

### 常用命令
```bash
# 重启容器（代码修改后）
cd ~/Workspace/Wolf_Fintrack && docker-compose restart collector

# 重建容器（环境变量修改后）
cd ~/Workspace/Wolf_Fintrack && docker-compose up -d collector

# 运行 DCF 分析
docker exec wolf_fintrack_collector python /app/scripts/dcf_valuation.py NVDA SOFI PLTR NVO JD --brief

# 同步到 Sheets
docker exec wolf_fintrack_collector python /app/scripts/dcf_to_sheets.py NVDA SOFI PLTR NVO JD

# 查看环境变量
docker exec wolf_fintrack_collector printenv | grep FINANCIAL
```

### API Keys 位置
- **config.json**: `api_keys.financial_datasets`
- **docker-compose.yml**: `FINANCIAL_DATASETS_API_KEY` 环境变量
- 两处都需要配置，环境变量优先

### 待优化
- [ ] 考虑把 API key 移到 `.env` 文件统一管理
- [ ] docker-compose.yml 的 `version` 字段已过时，可删除
- [ ] 可以添加 healthcheck 确保服务正常

---

## Tessera 同步

- 44 files pushed to GitHub (`vicenteliu/Tessera`)
- 包含多日 memory 文件、写作素材、项目资产
- 以后 heartbeat 会自动同步
- 新增 `.gitignore` 排除 `.openclaw/` 本地状态
